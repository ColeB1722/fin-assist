# treefmt — unified formatter configuration
# Used by: `just fmt`, `just check`, pre-commit hooks

[global]
excludes = ["flake.lock", ".devenv/*", ".direnv/*"]

[formatter.nixfmt]
command = "nixfmt"
includes = ["*.nix"]

[formatter.ruff]
command = "ruff"
options = ["format"]
includes = ["*.py"]
