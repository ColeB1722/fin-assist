eval "$(devenv direnvrc)"
use devenv

# Add uv-managed venv bin to PATH so tools like pytest, ruff, etc.
# are available directly (direnv propagates this to fish properly)
PATH_add .devenv/state/venv/bin
